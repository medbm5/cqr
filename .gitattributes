# Normalize to LF in the repository; the working copy stays platform-native.
* text=auto eol=lf

*.png binary
*.jpg binary
*.zip binary

# The case inputs are read-only source data: store them byte-for-byte so a
# clone matches the original archive on every platform.
data/** -text
